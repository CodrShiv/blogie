import {BASE_API_URL} from "./constants";
import { Button } from "@mui/material";

const Login = () => {
    <Button variant="contained" href={`${BASE_API_URL}/auth`}>
        LOGIN
    </Button>
}
export default Login;