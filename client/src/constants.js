const PRODUCTION = false,
  BASE_API_URL = PRODUCTION ? "https://blogie.ml" : "http://localhost:5000",
  CLIENT_URL = PRODUCTION ? "https://blogie.ml" : "http://localhost:3000";
export {BASE_API_URL, CLIENT_URL};
